<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.emt.shoppay.dao.mapper.IEpayOrderOperRecordMapper">

    <insert id="Insert" parameterType="java.util.HashMap">
      INSERT INTO epay_order_oper_record
        (orderId, payCompany, amount, status, orderDate, tranDate, comment, tranData, notifyData)
      VALUES
        (#{orderId},#{payCompany},#{amount},#{status},#{orderDate},#{tranDate},#{comment},#{tranData},#{notifyData})
    </insert>


</mapper>
